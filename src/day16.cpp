#include "common.h"

#include "inputs/day16.txt"

enum class Type
{
    len0 = 0,
    len1 = 1,
    literal = 4,
};

struct PacketHeader
{
    int8_t version;
    Type type;
};

int main()
{

    for (int i = 0; i < bits.size(); i++)
    {
    }
    return 0;
}